# 專案指南：精品電子商務網站

此專案為一個功能齊全的「精品電子商務網站」，採用 PHP/MySQL 技術架構，具備前台商品展示、購物流程以及後台管理系統。

## 專案架構概覽

- **前端介面 (`front/`)**：包含首頁、商品分類展示、商品詳情、會員註冊與登入、購物車及結帳功能。
- **後台管理 (`back/`)**：提供管理者登入、帳號管理、商品分類管理、商品上架/下架管理、訂單追蹤及新聞管理。
- **核心 API (`api/`)**：負責資料庫操作（CRUD）、購物車邏輯、圖片上傳、訂單儲存及權限檢查。
- **資產與資源 (`css/`, `js/`, `upload/`, `icon/`)**：存放樣式表、JavaScript 腳本、使用者上傳的商品圖片及系統圖示。

## 技術規格與開發指南

### 1. 資料庫操作 (`api/db.php`)
- 系統採用自定義的 `DB` 類別進行資料庫交互，使用 PDO 驅動。
- 全域變數如 `$Admin`, `$Mem`, `$Type`, `$Item`, `$Order` 分別對應資料庫中的不同資料表，簡化了資料查詢與操作。
- **重要**：所有資料庫變動應優先使用 `DB` 類別的方法（如 `save()`, `del()`, `find()`, `all()`）。

### 2. 購物與訂單邏輯
- **購物車**：使用 `$_SESSION['buycart']` 陣列存儲商品 ID 與數量。
- **訂單儲存**：訂單明細（商品 ID 與數量）在存入資料庫前，會透過 PHP 的 `serialize()` 函數進行序列化，讀取時需使用 `unserialize()`。

### 3. 權限控管 (`admin.php`)
- 管理者權限以序列化陣列形式儲存，對應後台不同的管理模組。
- 在 `admin.php` 中會根據 Session 中的權限設定來決定是否顯示特定的管理選單。

### 4. 商品與分類 (`back/th.php`)
- 支援多級分類（大分類、中分類）。
- 商品與分類的關聯透過分類 ID (big/mid) 進行鏈接。

## 開發規範與安全建議

- **SQL 注入防護**：系統已在 `api/db.php` 中使用 PDO 預處理語句，新增功能時請務必延續此做法。
- **檔案上傳**：商品圖片統一上傳至 `upload/` 目錄，請確保目錄具備寫入權限。
- **Session 安全**：敏感動作（如結帳、進入後台）應確實檢查 Session 狀態。

## 資料庫初始化
- 專案根目錄下的 `db13.sql` 與 `db13_20260223.sql` 包含資料庫結構與預設測試資料，部署前請先匯入。
